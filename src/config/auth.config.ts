/**
 * Authentication Configuration
 */
export const authConfig = {
  /**
   * If true: Users are automatically redirected to Keycloak login
   * If false: Users see the login page first and must click "Sign in with Keycloak"
   */
  autoRedirectToKeycloak: false, // Set to true for direct Keycloak login
};
